import turtle
x=turtle.Turtle()
pit = 50

def square(angle):
    x.forward(pit)
    x.right(angle)
    x.forward(pit)
    x.right(angle)
    x.forward(pit)
    x.right(angle)
    x.forward(pit)
    x.right(angle+10)

for i in range(36):
    square(90)